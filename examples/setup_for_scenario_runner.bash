#! /usr/bin/env bash

export CARLA_ROOT=$(realpath "../submodules/carla")
export AGENT_ROOT="${CARLA_ROOT}/PythonAPI/carla/"
export CARLA_EGGS=$(realpath "../carla_eggs")
export SCENARIO_RUNNER_ROOT=$(realpath "../submodules/scenario_runner")
